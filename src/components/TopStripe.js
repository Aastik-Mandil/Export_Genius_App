import React from "react";
import {Typography} from "@mui/material";

const TopStripe = () => {
  return (
    <Typography className="text-[16px] font-extrabold bg-primary uppercase font-[Montserrat Alternates] text-center">
      Turning uncertanity into your advantage
    </Typography>
  );
};

export default TopStripe